apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: iot-monitoring
spec:
  selector: { app: dashboard }
  ports:
    - name: backend
      protocol: TCP
      port: 3001
      targetPort: 3001
    - name: frontend
      protocol: TCP
      port: 5173
      targetPort: 5173
# manifests/50-backend/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: iot-monitoring
spec:
  selector: { app: backend }
  ports:
    - port: 3001
      targetPort: 3001
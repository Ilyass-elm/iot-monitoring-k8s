apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: iot-monitoring
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: mosquitto-config
    files:
      - mosquitto.conf